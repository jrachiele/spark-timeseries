apply plugin: 'java'
apply plugin: 'maven'

group = 'com.cloudera.sparkts'
version = '0.1.0-SNAPSHOT'

description = """Spark TS"""

sourceCompatibility = 1.7
targetCompatibility = 1.7



repositories {
        
     maven { url "http://repo.maven.apache.org/maven2" }
}
dependencies {
    compile group: 'org.scala-lang', name: 'scalap', version:'2.10.4'
    compile group: 'org.scala-lang', name: 'scala-compiler', version:'2.10.4'
    compile group: 'org.threeten', name: 'threeten-extra', version:'0.9'
    compile(group: 'org.scalanlp', name: 'breeze_2.10', version:'0.10') {
exclude(module: 'junit')
exclude(module: 'commons-math3')
    }
    compile(group: 'org.scalanlp', name: 'breeze-viz_2.10', version:'0.10') {
exclude(module: 'junit')
exclude(module: 'commons-math3')
    }
    compile group: 'org.apache.commons', name: 'commons-math3', version:'3.4.1'
    testCompile group: 'org.scalatest', name: 'scalatest_2.10', version:'2.2.1'
    testCompile group: 'junit', name: 'junit', version:'4.11'
    compile(group: 'org.apache.hadoop', name: 'hadoop-yarn-client', version:'2.6.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.scala-lang', name: 'scala-library', version:'2.10.4') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-core_2.10', version:'1.3.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-sql_2.10', version:'1.3.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-catalyst_2.10', version:'1.3.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
    compile(group: 'org.apache.spark', name: 'spark-mllib_2.10', version:'1.3.1') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
